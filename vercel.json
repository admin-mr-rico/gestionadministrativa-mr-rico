{
  "buildCommand": "npm --prefix src run build",
  "installCommand": "npm --prefix src install",
  "outputDirectory": "src",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}